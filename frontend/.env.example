# Example environment variables for Monte Carlo Frontend

# The base URL for the backend API
# For local development with Docker and Nginx proxy, this can be /api
# For other setups, it might be a full URL like http://localhost:8000/api
VITE_API_URL=/api

# Set to true to enable debug features or more verbose logging in the frontend (if applicable)
# VITE_DEBUG=true